const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith('.tsx')) results.push(file); } }); return results; } const dirs = ['src/features/chat/components', 'src/features/profile/components', 'src/features/dashboard/components', 'src/features/notifications/components']; let files = []; dirs.forEach(d => { if(fs.existsSync(d)) files = files.concat(walk(d)) }); files.forEach(file => { let content = fs.readFileSync(file, 'utf8'); content = content.replace(/bg-white\/60/g, 'bg-card/60').replace(/bg-white/g, 'bg-card').replace(/bg-slate-50/g, 'bg-muted').replace(/bg-slate-100/g, 'bg-muted/50').replace(/border-slate-100/g, 'border-border').replace(/border-slate-200/g, 'border-border').replace(/border-white/g, 'border-border').replace(/text-slate-900/g, 'text-foreground').replace(/text-slate-800/g, 'text-foreground').replace(/text-slate-700/g, 'text-foreground').replace(/text-slate-600/g, 'text-muted-foreground').replace(/text-slate-500/g, 'text-muted-foreground').replace(/text-slate-400/g, 'text-muted-foreground'); fs.writeFileSync(file, content); });
